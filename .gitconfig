[user]
	name = Riley Voor
	email = rvoor@neadwerx.com
[color]
	status = always
	diff = auto
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
